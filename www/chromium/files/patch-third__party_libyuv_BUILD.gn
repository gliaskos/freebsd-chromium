--- third_party/libyuv/BUILD.gn.orig	2016-08-03 22:03:43.000000000 +0300
+++ third_party/libyuv/BUILD.gn	2016-09-19 01:42:05.817433000 +0300
@@ -14,6 +14,9 @@
     ".",
     "include",
   ]
+  if (is_bsd) {
+    include_dirs += ["/usr/local/include"]
+  }
 }
 
 use_neon = current_cpu == "arm64" || (current_cpu == "arm" && (arm_use_neon || arm_optionally_use_neon))
